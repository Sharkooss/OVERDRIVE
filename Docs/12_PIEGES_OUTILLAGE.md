# 12 — PIÈGES D'OUTILLAGE & ERREURS DÉJÀ COMMISES

> **Lis ce fichier avant de toucher à un Blueprint, un asset ou l'éditeur.**
> Chaque ligne ici a coûté du temps de production. Aucune n'est théorique.
>
> **Tu ajoutes une entrée à chaque fois que tu tombes dans un piège** — outil qui ment,
> erreur silencieuse, ou bêtise de ta part. Une entrée = symptôme observable + cause + parade.
> Un piège non écrit sera refait par le prochain agent (`Docs/00_INDEX.md`, R9 de `CLAUDE.md`).

Légende de gravité : 💀 destructeur · 🔴 casse le jeu en silence · 🟠 fait perdre du temps · ⚪ cosmétique

---

## 1. Les trois règles qui résument tout

1. **Un outil qui ne renvoie pas d'erreur n'a pas forcément fait ce que tu crois.**
   Après *toute* écriture, relis l'état réel — et pas via l'outil qui vient d'écrire.
   **Compter les nœuds ne suffit pas** : le writer peut en *ajouter un* pour faire coller un type
   (2.21). Relis les **connexions**, pas seulement le nombre.
2. **Ne supprime jamais en masse sans lister ce que tu vas supprimer et le faire valider.**
   Deux destructions accidentelles en 3 jours, les deux par « nettoyage automatique ».
3. **Une preuve qui contredit ton hypothèse est une preuve, pas une anomalie.**
   Si une valeur ne colle pas, cherche pourquoi *avant* de conclure « c'est l'environnement ».

---

## 2. Blueprint — DSL de graphe (`write_graph_dsl` / `read_graph_dsl`)

C'est l'outil le plus puissant du projet **et le plus traître**. 100 % des régressions du J3 viennent d'ici.

| # | Gravité | Piège | Symptôme | Parade |
|---|---|---|---|---|
| 2.1 | 💀 | **`read_graph_dsl` ne suit que le pin d'exec par défaut.** Les branches sur `Triggered` / `Started` / `Completed` des events Enhanced Input sont **invisibles**. | `BP_PlayerCharacter:EventGraph` se lit comme 5 events au corps vide alors qu'il contient 19 nœuds. Un `write_graph_dsl` dessus **efface tout le câblage d'input**. | Avant d'écrire : `find_nodes(graph, title="")`. Si le nombre de nœuds dépasse ce que montre le DSL → **interdiction d'écrire en DSL**, passer par `create_node` + `connect_pins`. |
| 2.2 | 💀 | **`write_graph_dsl` EMPILE, il n'écrase pas.** Les anciens nœuds restent, orphelins. | L'`EventGraph` de `BPC_MovementState` avait **101 nœuds / 5 copies** de sa boucle de Tick, accumulées sur 2 jours. `read_graph_dsl` n'en montrait qu'une : les orphelines masquaient l'état réel. | Compter les nœuds après chaque écriture. Purger par accessibilité **exec** (cf. 2.3). |
| 2.2b | 💀 | **~~Sur un graphe de fonction, l'écriture remplace correctement.~~ FAUX — corrigé au J4.** L'empilement frappe **aussi** les graphes de fonction, mais **pas systématiquement** : sur 5 réécritures du J4, 3 ont proprement remplacé et 2 ont empilé (`SetSlideInput` +6 nœuds, `CanEnterState` **+74**, soit une copie entière de la table d'états). | `CanEnterState` est passé de 75 à 149 nœuds sans une seule erreur. `read_graph_dsl` ne montrait que la chaîne vivante — l'ancienne était invisible. | **Compter les nœuds avant et après chaque `write_graph_dsl`, sans exception.** Si le total a augmenté, purger par accessibilité exec. Ne jamais déduire « ça a remplacé » d'une lecture qui a l'air correcte. |
| 2.3 | 💀 | **`find_nodes(entry_points_only=True)` ne renvoie PAS les events Enhanced Input.** | Une purge « supprime tout ce qui n'est pas atteignable depuis un point d'entrée » **a supprimé les 5 events d'input** de `BP_PlayerCharacter`. | Traiter les `K2Node_EnhancedInputAction` comme des racines **à la main**. Et lister les nœuds à supprimer avant de le faire. |
| 2.3b | 🔴 | **Le `bind` du DSL ne fait PAS une copie de valeur.** `(bind x (Variables\|…\|GetFoo))` crée un nœud **pure**, évalué **quand sa sortie est tirée** — donc *après* un `Set` écrit plus haut dans la chaîne d'exec. Toute détection de front écrite naïvement est morte. | `SetSlideInput` : `(bind was (GetbSlideHeld))` puis `(SetbSlideHeld bHeld)` puis `(if (and bHeld (not was)) …)`. Le `Branch` tire `was` **après** l'écriture → `was == bHeld` → condition **toujours fausse**. Le slide n'a jamais pu se déclencher, sans une seule erreur nulle part. | **Ne jamais lire une variable après l'avoir écrite dans la même fonction.** Faire la comparaison **avant** toute écriture (mettre le `Set` dans les branches du `if`), ou passer par une vraie variable locale. Vérifier dans le graphe que le `Branch` précède le `Set` dans la chaîne d'exec. |
| 2.3c | 🔴 | **Rappel — 2.3b est revenu au J5, sous une forme qu'un audit des connexions ne voit pas.** `ResumeSlideIfNeeded` faisait `(SetbWasSlidingBeforeDash false)` **puis** `(if (and _was _held) …)`. Le graphe était parfaitement câblé : le bug est dans l'**ordre d'exécution** entre un `Set` et un getter **pur** tiré plus tard. Condition toujours fausse, fonction morte pendant deux playtests. | Un audit « chaque `self` vient-il de la bonne variable ? » (2.21) ne le détecte **pas**. | **Vérification supplémentaire après écriture** : pour chaque `Branch`/`Select`, remonter sa condition jusqu'aux getters, et vérifier qu'aucun `Set` de la **même variable** ne le précède dans la chaîne d'exec. En pratique : **ne jamais écrire un `Set` avant un `if` qui lit la même variable dans le même corps.** Ici la bonne réponse était de supprimer le reset — la variable était déjà réécrite en amont à chaque passage. |
| 2.4 | 🟠 | **`get_connected_subgraph` suit aussi les liens de données** — inutilisable pour détecter du code mort. | Il renvoyait 101/101 nœuds « connectés » : les chaînes orphelines partagent le pin `DeltaSeconds` du même Event Tick. | Calculer soi-même : BFS sur les pins **Exec** depuis les racines, puis remonter les alimentations de données des nœuds vivants. |
| 2.5 | 🔴 | **Argument positionnel sur une fonction membre → il part sur le pin `self`.** S'il est incompatible, il est **jeté en silence** et le vrai pin garde son défaut. | `(SetComponentTickEnabled true)` a laissé `bEnabled = false` → **Tick du composant désactivé, 3 features mortes, 0 erreur, compilation OK**. | **Toujours nommer les pins** : `(CallFunction|Foo :NomDuPin valeur)`. Sans exception. |
| 2.6 | 🔴 | **La sortie de `read_graph_dsl` n'est pas réinjectable.** Elle écrit en positionnel (cf. 2.5). | Recopier `(CallFunction|UpdateSpeedCap DeltaSeconds)` échoue ou câble faux. | Jamais de round-trip aveugle read → edit → write. Renommer tous les pins en réécrivant. |
| 2.7 | 🔴 | **Collision fonction / variable homonyme : le writer choisit la variable, en silence.** | `(CallFunction|SetHorizontalSpeed …)` a produit le *setter de la variable* `HorizontalSpeed` : l'affichage bougeait, la vélocité non. | Éviter de nommer une fonction `SetX` quand une variable `X` existe. Sinon, écrire la logique en clair (ici : `Class|MovementComponent|SetVelocity`). |
| 2.8 | 🟠 | Les **setters de variables** veulent le chemin de catégorie complet et **sans underscores**. | Le lecteur affiche `(\|SetTune_SpeedWalk …)`, mais il faut écrire `(Variables\|Movement\|Cached\|SetTuneSpeedWalk …)`. Les booléens perdent leur `b` : `GetIsGrounded`. | `find_node_types(graph, filtre)` avant d'écrire. |
| 2.9 | ⚪ | Les `type_id` à **parenthèses** fonctionnent (`Math\|Trig\|Cos(Degrees)`, `Utilities\|String\|ToString(Float)`) — contrairement à ce qu'affirmait le J2. | — | C'est l'argument **positionnel** qui casse, pas la parenthèse. Nommer le pin (`:A`, `:InDouble`). |
| 2.10 | 🟠 | `Transformation\|GetVelocity` (ce que le lecteur affiche) **refuse un CMC** en `self`. | `Could not connect pin CachedCMC to self`. | `Class\|MovementComponent\|GetVelocity`. |
| 2.11 | 🟠 | Un `if` **termine le flux d'exec** : ce qui suit dans le même corps ne s'exécute pas. | — | Mettre l'`if` en dernier, ou extraire en fonction. |
| 2.12 | ⚪ | `get_node_type_pins` **instancie un nœud temporaire** dans le graphe interrogé. | Nœuds orphelins qui traînent (un `ToString(Boolean)` est resté 2 jours dans `BP_PlayerCharacter`). | Interroger un graphe qui sera réécrit, ou nettoyer après. **Nuance J8quater : 9 sondes successives sur `BPC_WallRide:CheckDetachInput` n'ont laissé AUCUN nœud** (32 avant, 32 après, comptés par `find_nodes`). Le nettoyage a donc lieu au moins parfois. **Ne pas s'y fier** : compter avant/après reste la seule preuve, et ça ne coûte qu'un appel. |
| 2.13 | ⚪ | `arrange_nodes` ne fait rien (0 nœud déplacé, sans erreur). | — | Positionner via l'argument `pos` de `create_node`. |
| 2.14 | 🟠 | **Le `type_id` que `get_node_infos` affiche pour un event Enhanced Input n'est pas celui que `create_node` accepte.** Le lecteur dit `Input\|EnhancedActionEvents\|EnhancedInputActionIA_Slide` ; `create_node` répond « does not exist ». | — | Le vrai id est `Input\|EnhancedActionEvents\|IA_Slide`. **Toujours passer par `find_node_types(graph, "IA_")`** au lieu de recopier ce que le lecteur affiche. Vaut aussi pour `EnhancedActionValues`. |
| 2.15 | 🟠 | **Les pins « float » du Blueprint s'appellent `InDouble`, pas `InFloat`.** `Utilities\|String\|ToString(Float)` prend `:InDouble`. | `Unknown input pin "InFloat"` — au moins l'erreur est explicite et bloque l'écriture. | `get_node_type_pins` avant d'écrire tout nœud de conversion. Le message d'erreur liste les pins valides : le lire au lieu de deviner. |
| 2.16 | 🟠 | `get_node_type_pins` / `find_node_types` sur un `type_id` inexistant **lève une exception qui avorte tout le script** `execute_tool_script` (les appels précédents, eux, ont bien été exécutés). | Un `remove_variable` en début de script part, puis le script meurt sur une sonde ratée → état intermédiaire. | Envelopper **chaque** sonde dans un `try/except Exception` (pas seulement `RuntimeError` : le binding en lève d'autres). Ne jamais mélanger sondes et écritures dans le même script. |
| 2.17 | ⚪ | `add_variable` n'accepte qu'une liste fermée de types : `bool, int, float, byte, name, string, text, Vector, Rotator, Transform, Vector2D, LinearColor`. **`double` est refusé** alors que le float Blueprint d'UE 5.8 *est* un double. | — | Utiliser `float` : le moteur crée bien un double. Pour un type objet, `add_object_variable` ; pour un struct, `add_struct_variable`. |
| 2.18 | 🔴 | **`remove_function_param` ne nettoie pas les nœuds d'appel existants.** Le pin supprimé reste sur chaque `CallFunction` déjà posé → `Pin X named X doesn't match any parameters of function Y` à **chaque** compilation, y compris celles déclenchées par un `write_graph_dsl` sur un **autre** graphe. Tout le Blueprint devient inécrivable. | Une réécriture parfaitement valide échoue avec une erreur qui désigne un graphe qu'on ne touche pas. | Après un `remove_function_param` : chercher les nœuds d'appel de cette fonction dans **tous** les graphes (`find_nodes` + `get_node_infos`, filtrer sur le `type_id`) et les **supprimer** — `delete_node` ne recompile pas, donc il passe même quand le BP est en erreur. Puis réécrire les graphes appelants. |
| 2.19 | 🟠 | **`find_node_types` ment sur ce qui vient d'être créé.** Un composant ajouté à un Blueprint, ou un `BPC_*` tout neuf, **n'apparaît pas** dans la base d'actions — même après `compile_blueprint` **et** `save_assets`. `find_node_types(EventGraph, "BPCDash")` renvoie `[]` alors que la propriété `dash` existe bien sur le CDO. | J5 : `Variables|Default|GetDash` et `Class|BPCDash|TryDash` introuvables pendant plusieurs minutes. Conclure « le composant n'a pas été ajouté » aurait été **faux**. | **La base d'actions n'est pas la vérité, la classe l'est.** Vérifier par `ObjectTools.list_properties` sur le `Default__<BP>_C` (ou `ActorTools.get_components`). Puis **écrire le `type_id` à la main** : `create_node` / `write_graph_dsl` résolvent parfaitement des ids que `find_node_types` ne liste pas. Ne jamais renoncer à un id sur la foi d'un `find_node_types` vide. |
| 2.20 | ⚪ | **`find_node_types` exige `context_pins`** (même vide) et `break_pins` exige `output_pin` **et** `input_pin` — pas un unique `pin`. Le schéma affiché par `describe_toolset` ne rend pas ces obligations évidentes. | `input param "context_pins" is required … but is missing`. Le script entier avorte (cf. 2.16). | Passer `"context_pins": []`. Pour `break_pins`, fournir les deux extrémités du lien à casser. Extension de 4.7. |
| 2.21 | 💀 | **Quand le type ne colle pas sur `self`, le writer DSL n'échoue pas : il INVENTE un chemin.** Il cherche, sur l'objet fourni, une propriété du type attendu et **insère silencieusement un getter intermédiaire**. Le graphe compile, le nœud demandé est là, ses pins sont nommés — et il opère sur un objet que tu n'as jamais désigné. Pire que 2.5 : là où 2.5 *jette* l'argument, 2.21 en **fabrique un autre**. | J5 : `(Pawn\|GetControlRotation :self _char)` avec `_char` = `Character`. Le `type_id` `Pawn\|GetControlRotation` est **ambigu — `find_node_types` le liste deux fois** — et le writer a résolu vers `AController::GetControlRotation`, dont le `self` est un **Controller**. Pour satisfaire le type il a intercalé `CachedCharacter → GetLastHitBy` (`APawn::LastHitBy` *est* un `Controller`). `LastHitBy` est nul hors combat → rotator nul → **le dash partait toujours vers `(1,0,0)`**, quel que soit le regard. Zéro erreur, zéro warning, compilation verte. | **Après chaque écriture, relire le graphe nœud par nœud et vérifier que chaque `self` est alimenté DIRECTEMENT par la variable voulue.** Tout nœud intermédiaire que tu n'as pas écrit est un bug. Contrôle bon marché : comparer la liste des `type_id` présents à ceux que tu as écrits — un getter inconnu = une insertion. Et **fuir les `type_id` ambigus** : ici, remplacé par `(Transformation\|GetForwardVector :self CachedCamera)`, dont le `self` est un `SceneComponent` que la caméra satisfait directement. Vérifier alors la sémantique (`bUsePawnControlRotation = true` sur la caméra, sinon plus de pitch). |

| 2.22 | 🟠 | **Un pin de type ARRAY ne peut pas être alimenté depuis le DSL.** `(Collision\|LineTraceForObjects … :ObjectTypes (Utilities\|Array\|MakeArray "ObjectTypeQuery8"))` échoue : *« Could not connect pin Array to ObjectTypes »*. Le `MakeArray` naît en **wildcard** ; tant qu'il n'est pas connecté il ne sait pas qu'il contient des `EObjectTypeQuery`, et le littéral ne peut pas s'y poser. | J6 : 3 traces d'objets à câbler dans `BPC_WallRide`. | **Ordre obligatoire, hors DSL** : (1) `create_node(MakeArray)` ; (2) `connect_pins(MakeArray.Array → …ObjectTypes)` — c'est **la connexion** qui résout le wildcard, vérifié : le pin `[0]` passe de `Wildcard` à `EObjectTypeQuery Enum` ; (3) `set_pin_value([0], "ObjectTypeQuery8")` ; (4) `add_node_pin` pour chaque élément supplémentaire. Écrire la fonction en DSL **sans** l'argument `:ObjectTypes`, puis rattraper les nœuds `Collision\|LineTrace*` par `find_nodes` + `get_node_infos` et brancher. |
| 2.23 | 🟠 | **Une écriture DSL qui laisse le Blueprint non compilable lève une exception… après avoir écrit les nœuds.** `write_graph_dsl` déclenche une compilation ; si elle échoue, l'exception remonte et **avorte le script `execute_tool_script`** — donc le comptage de nœuds avant/après (2.2b) ne s'exécute jamais, alors que les nœuds sont bien là. | J6 : 4 fonctions écrites d'un coup, script mort sur le 1ᵉʳ `ObjectTypes` non connecté. Croire que « rien n'a été écrit » aurait conduit à tout réécrire → empilement garanti. | **Après toute erreur de compilation renvoyée par `write_graph_dsl`, recompter les nœuds dans un appel séparé avant de retenter.** Corollaire de 2.16 : ne jamais déduire l'état du graphe de l'échec de l'appel. |
| 2.2c | 💀 | **Le test « y a-t-il des nœuds orphelins ? » est FAUX s'il cherche des nœuds totalement déconnectés.** Une chaîne empilée par `write_graph_dsl` (2.2b) est **connectée à elle-même** : seul son *premier* nœud a une entrée exec libre, tous les suivants ont leurs deux pins branchés. Un filtre « aucune connexion exec » ne voit donc **rien**, et le graphe compile sans un warning. | J7, `AddSpeedGain` : 14 → **42** nœuds. Mon premier contrôle a répondu « zéro orphelin » alors que **deux chaînes complètes de 7 nœuds** coexistaient. Seul le comptage (42 pour une fonction de 14 lignes) a mis la puce à l'oreille. | **Le seul test valable est l'accessibilité** : BFS sur les pins **Exec** depuis le nœud d'entrée, puis remontée des alimentations de **données** des nœuds vivants ; tout le reste est mort (algorithme de 2.4). Contrôle bon marché en attendant : **lister les `type_id` des nœuds portant un pin Exec** — si un nom apparaît deux fois, il y a deux chaînes. Ici : `SetLastGainAmount` ×2. |
| 2.25 | 🟠 | **`(bind x …)` ne garantit PAS un nœud unique : le writer réinjecte l'expression à chaque usage.** La doc du DSL insiste pour binder plutôt que répéter un appel — mais le writer **inline le bind** et recrée le sous-graphe complet à chaque référence. | J7, `TryBunnyHop` : 6 `bind` sur `GetCachedCMC` / `GetVelocity` / `VectorLengthXY` ont produit **53 nœuds** au lieu de ~30, avec 5 copies de `GetVelocity`. Aucune erreur. | **Sans conséquence pour des nœuds purs** (ils relisent la même source au même instant) — mais **dangereux dès qu'un nœud impur est bindé** (`Random`, trace physique, `GetGameTimeInSeconds`) : chaque copie s'exécute séparément et renvoie une valeur différente. Et **fatal si un `Set` s'exécute entre deux relectures** : la 2ᵉ copie lit la valeur écrite (famille 2.3b). Règle : après écriture, **compter les nœuds** ; si le total dépasse largement l'attendu, vérifier qu'aucune valeur relue ne traverse un `Set`. |
| 2.26 | ⚪ | **Un appel de dispatcher d'événement échoue en DSL avec un message trompeur.** `(Default|CallOnSpeedGained _gain "BunnyHop")` → *« Could not connect pin ReturnValue to self »*. Le writer traite le 1ᵉʳ argument positionnel comme un `self` (famille 2.5), alors que le nœud n'en a pas. | J7. L'écriture **entière** est annulée : relecture après coup, le graphe ne contenait que son nœud d'entrée — contrairement à 2.23 où les nœuds restent. | Les deux comportements existent : **toujours recompter après un échec**, ne jamais supposer lequel s'est produit. Pour un dispatcher, nommer les pins ou le poser en `create_node` + `connect_pins`. |
| 2.27 | 🟠 | **`write_graph_dsl` ne sait PAS créer un custom event.** `(event TryFire …)` → *« AssertionError: The node could not be created / AddEvent\|TryFire does not exist »*. Le DSL ne résout `(event X)` que vers un `AddEvent\|X` **existant dans la base d'actions**, c'est-à-dire un override natif. | J8 : l'écriture entière des 3 events de `BP_LaserWeapon` est morte sur le 1ᵉʳ custom event. Relecture après coup : **0 nœud** dans le graphe (comportement 2.26, pas 2.23). | **Créer d'abord le nœud avec `BlueprintTools.add_event(blueprint, name, position)`** — idempotent, il renvoie le nœud existant s'il y en a un. Son `type_id` réel est alors `AddEvent\|Custom\|<Nom>`. En DSL, écrire **`(event Custom|TryFire …)`** : le writer **réutilise** le nœud déjà là au lieu d'en créer un second (vérifié : 2 events custom préexistants + une écriture DSL = toujours 2 nœuds d'event). |
| 2.28 | 🟠 | **Le pin d'événement d'un `Set Timer by Event` s'appelle `Delegate`, et aucun DSL ne l'alimente.** Il n'apparaît pas non plus comme argument possible : `:Delegate MonEvent` n'a pas de sens, le DSL n'a pas de littéral délégué. | J8, cooldown de tir : le timer était posé, `Time` câblé, `Delegate` vide → **le timer n'aurait jamais rappelé `EndFireCooldown`**, `bCanFire` serait resté `false` après le premier tir. Zéro erreur, compilation verte. | Pas besoin d'un nœud `Create Event` : **connecter directement le pin de sortie `OutputDelegate` du `K2Node_CustomEvent` au pin `Delegate` du timer** avec `connect_pins`. Après toute écriture d'un `SetTimerbyEvent`, **relire son pin `Delegate` et vérifier qu'il est connecté** — c'est un pin muet qui ne provoque aucun warning quand il est vide. |
| 2.29 | ⚪ | **Les pins d'un `Make`/`Break` de `UserDefinedStruct` portent un suffixe GUID.** `Utilities\|Struct\|MakeSDamageInfo` n'a pas de pin `Amount` mais `Amount_16_211B3B4D4D1E82B0C0E57D9D7A0BF69E`. | Nommer `:Amount` échoue. | `get_node_type_pins` sur le `Make` **avant** d'écrire, et recopier les noms au caractère près. Ils sont stables tant que le struct n'est pas réordonné — **ils changent si Louis réordonne les champs**, donc les resonder après toute retouche du `S_*`. |
| 2.30 | ✅ | **Le writer DSL déduplique les `bind` de nœuds impurs — contre-exemple à 2.25.** Au J8, `(bind (hit blocking) (CallFunction\|ResolveShot))` avec `hit` référencé 4× et `blocking` 3× a produit **un seul** nœud `ResolveShot` (donc **un seul** trace physique par tir). Idem pour `BreakHitResult` référencé 3× dans `ProcessHit` : 1 nœud. | — | 2.25 reste vrai (il a été observé au J7) : les deux comportements existent selon la forme. **La règle ne change pas : compter les nœuds après écriture et chercher les doublons de `type_id` porteurs d'effet de bord.** Ce qui change, c'est qu'un `bind` d'appel de fonction impure **n'est pas** automatiquement disqualifié. |
| 2.31 | 🔴 | **Un audit d'accessibilité exec maison redéclenche 2.3 s'il définit ses racines par `type_id`.** Un filtre « racine = `type_id` commençant par `AddEvent\|` » ignore les `K2Node_EnhancedInputAction` (dont le `type_id` commence par `Input\|EnhancedActionEvents\|`) **et** les entrées de fonction. | J8 : mon auditeur a annoncé **27 nœuds morts sur 40** dans `BP_PlayerCharacter:EventGraph` — soit tout le câblage d'input du J2 au J7. Une purge automatique aurait rejoué **mot pour mot** la destruction 3.2. | **Définir la racine par la TOPOLOGIE, jamais par le nom** : est racine tout nœud qui a au moins une sortie `Exec` et **aucune** entrée `Exec`. Couvre events natifs, custom events, events d'input et entrées de fonction, sans liste à maintenir. Et corollaire de 3.2 : **un auditeur ne supprime rien**, il imprime une liste. |
| 2.32 | 🟠 | **`read_graph_dsl` DÉDOUBLE à la lecture ce que le writer a factorisé : il ré-inline un nœud pur partagé à chaque endroit où sa sortie est consommée.** Le DSL relu montre donc N copies d'un nœud qui n'existe **qu'une fois** dans le graphe. C'est le miroir exact de 2.25 (où le writer, lui, duplique vraiment) — et les deux se ressemblent à l'écran. | J8bis, `UpdateBeam` : le DSL relu affiche **trois** `(Utilities\|Struct\|BreakLinearColor (GetDebugLineColor))` pour alimenter R, G et B. Il n'y en a **qu'un** — 17 nœuds au total, et `get_node_infos` montre un unique `K2Node_BreakStruct_2` dont les trois sorties partent vers le `MakeLinearColor`. Conclure « le writer a dupliqué, il faut purger » aurait détruit un graphe correct. | **Ne jamais compter les nœuds dans le DSL relu : compter avec `find_nodes`.** Le DSL dit la *logique*, `get_node_infos` dit la *topologie* — et seule la topologie est la vérité (extension de 2.6). Corollaire pratique : un doublon apparent dans le DSL relu ne se diagnostique qu'en comparant `find_nodes` à l'attendu. Si le total colle, il n'y a pas de doublon, quoi que montre le DSL. |
| 2.33 | 🔴 | **Un getter de variable appartenant à une AUTRE classe Blueprint n'est créable que sous la forme `Class\|<ClasseSansUnderscores>\|Get<VariableSansUnderscores>`. Les DEUX autres formes que l'outillage affiche sont INUTILISABLES**, en `create_node` comme en `write_graph_dsl` : `read_graph_dsl` montre `(\|GetSlide_TurnRate _md)` et `find_node_types(…, context_pins=[pin PDA])` renvoie `Variables\|Movement\|Slide\|GetSlideTurnRate` — les deux répondent *« does not exist »*. Et `find_node_types` **sans** `context_pins` n'en liste **aucun** : ni le neuf, ni ceux déjà posés dans le graphe qu'on interroge. | J8bis : lire `Slide_HeadingFollowSpeed` depuis `PDA_MovementData` dans `BPC_Slide.CacheTuning`. **9 formes d'id essayées, 8 échecs**, y compris `declaring_class` sur la classe générée (`…_C`) qui ne change rien. Le contrôle décisif a été de retenter avec un `type_id` **déjà présent dans le graphe** (`\|GetSlide_TurnRate`) : lui non plus n'est créable — donc ce n'était pas « la variable est trop neuve » (2.19/5.20), c'était **la forme de l'id**. | **Écrire `Class\|<Classe>\|Get<Var>` en retirant tous les underscores des deux noms.** Ici : variable `Slide_HeadingFollowSpeed` de la classe `PDA_MovementData` → **`Class\|PDAMovementData\|GetSlideHeadingFollowSpeed`**. Contrôle après création : `get_node_infos` renvoie alors le `type_id` **d'affichage** `\|GetSlide_HeadingFollowSpeed` et un pin `self` du bon type — c'est normal, l'id d'écriture et l'id de lecture ne sont pas le même (extension de 2.6 et 2.8). Corollaire : **ne jamais conclure « impossible » d'un échec de `create_node` sur l'id qu'affiche le lecteur.** |
| 2.34 | ✅ | **Insérer un maillon dans une chaîne d'exec existante est plus sûr que réécrire le graphe en DSL — et c'est parfois la SEULE option.** `BPC_Slide:CacheTuning` (57 nœuds) relu en DSL contient `(Class\|CapsuleCollision\|GetCapsuleHalfHeight _movementdata)` : un **artefact du lecteur** (2.6) pour un simple getter de variable du DataAsset. Réinjecter ce DSL aurait posé de **vraies fonctions moteur de capsule** avec un DataAsset en `self` — 2.21 à l'échelle de 6 clés de tuning. | J8bis. La réécriture DSL était le réflexe ; elle aurait été destructrice. | Recette, 5 appels, zéro risque d'empilement (2.2b) : (1) `create_node` des nouveaux nœuds ; (2) `break_pins(A.then → B.execute)` sur le maillon où insérer ; (3) `connect_pins(A.then → N.execute)` ; (4) `connect_pins(N.then → B.execute)` ; (5) alimenter les pins de données depuis les nœuds **déjà présents**. Puis **auditer la chaîne** : un seul nœud racine (2.31), longueur de chaîne = attendu, total de nœuds = avant + créés. Ici `57 → 59`, chaîne de 33, un seul root. |
| 2.24 | 🟠 | **Deux conventions de nom pour la même variable booléenne, selon l'outil.** Le `type_id` du nœud variable **perd le `b`** : `Variables\|WallRide\|SetIsWallRiding`, `GetTuningCached`. Le nom de **propriété réflexion** (pour `ObjectTools.get_properties`) le **garde** et passe en lowerCamel : `bIsWallRiding`, `bTuningCached`. | `get_properties([... "isWallRiding"])` → *« the following properties could not be read »*. `find_node_types("Variables\|WallRide\|")` → `SetIsWallRiding`. | Retenir la règle : **nœud de graphe → sans `b`** (confirme 2.8), **introspection → avec `b`**. En cas de doute, `list_properties` sur l'instance donne la liste exacte des noms réflexion. |

### Le contrôle à 1 appel qui tranche un `type_id` ambigu (complément de 2.21)

Quand `find_node_types` liste deux fois le même id (`Pawn|GetControlRotation`, `Utilities|IsValid`),
on n'a pas besoin de deviner lequel le writer a choisi : **le type déclaré du pin `self` le dit.**

```
Pawn|GetControlRotation  self = "Controller Object Reference"  → AController::GetControlRotation  ✅
Pawn|GetControlRotation  self = "Pawn Object Reference"        → APawn::GetControlRotation        ❌
```

Et comme un `PlayerController` **n'est pas** un `Pawn`, si le writer avait pris la mauvaise surcharge
il aurait **dû** insérer un nœud intermédiaire (2.21) — donc :
**`self` alimenté DIRECTEMENT par la variable voulue + type de pin attendu = preuve.**
Vérifié au J8 sur `ResolveShot` : `GetOwnerController → GetControlRotation.self`, pin `Controller`,
zéro nœud intercalé. Deux lignes de `get_node_infos` remplacent une journée de doute.

### La vérification qui aurait tout évité

Un graphe qui compile ne prouve rien. **Après écriture, relire les *valeurs de pins*, pas le DSL :**

```python
infos = get_node_infos(nodes)
# pour chaque pin d'entrée non-Exec et NON CONNECTÉ, afficher sa valeur littérale
```

⚠️ Un filtre « non connecté **et** valeur vide » **ne suffit pas** : un argument perdu laisse le
défaut du pin — `false`, `0.0` — qui n'est pas vide. C'est précisément ce qui a laissé passer 2.5.

---

## 3. Destructions accidentelles

| # | Gravité | Ce qui s'est passé | Leçon |
|---|---|---|---|
| 3.1 | 💀 | **J2** — suppression de `Content/Input/` : 9 assets emportés au lieu des 6 annoncés (le dossier contenait aussi `Touch/`). | Lister le contenu réel **avant** de supprimer un dossier, pas ce qu'on croit qu'il contient. |
| 3.2 | 💀 | **J3** — une purge de nœuds morts a supprimé les 5 events Enhanced Input (cf. 2.3). Corrigé en les reconstruisant un par un. | Toute suppression en masse : produire la liste, la vérifier, **puis** supprimer. Jamais dans le même appel. |
| 3.3 | 🟠 | **J3** — commit effectué avant validation de Louis, sur une régression qui cassait tout le jeu. | **Ne jamais committer une feature de gameplay avant que Louis l'ait jouée.** Sauvegarder les assets, s'arrêter, attendre. Cf. R8 + R10. |

---

## 4. Vérification & PIE

| # | Gravité | Piège | Parade |
|---|---|---|---|
| 4.1 | 🔴 | **Expliquer une preuve contradictoire par « l'environnement ».** Le relevé PIE montrait `MaxAcceleration = 2048` (défaut moteur) au lieu de 4000 : le Tick était mort. J'ai conclu « l'éditeur n'a pas le focus » et commité. | Une hypothèse d'environnement **se teste**. Ici, `CMC.MovementMode = MOVE_Walking` prouvait que le monde tickait — une seule propriété séparait le bon du mauvais diagnostic. |
| 4.2 | 🟠 | `bThrottleCPUWhenNotForeground` (Editor → General → Performance) fait tourner PIE à **3 tick/s** sans focus. **Mis à `false` le 2026-08-19**, réglage local non versionné. | Vérifier `max tick rate 60` dans le log après `StartPIE`. Sur un nouveau poste, le remettre à `false`. |
| 4.3 | 🔴 | `SlateInspectorToolset.PressKey` n'atteint le jeu que si **`GameGetsMouseControl = true`** (Editor → LevelEditor → PlayIn). Sinon : faux négatif. | L'activer **le temps du test**, puis le remettre à `false` — sinon le PIE capture la souris pendant les playtests de Louis. |
| 4.4 | 🟠 | **Un aller-retour MCP fait avancer ~20 s de temps de jeu.** Impossible de tester une fenêtre inférieure à la seconde (buffer de saut, coyote time, i-frames). | Viser une **trace persistante** (`bDebugEnabled`, `PreviousState`, `LandedTime`), jamais une grandeur instantanée. Pour les fenêtres courtes : monter temporairement la valeur de tuning, vérifier le mécanisme, restaurer. Sinon, **le dire** et déléguer à Louis. |
| 4.5 | 🟠 | `time.sleep()` dans `execute_tool_script` **gèle le jeu** : les appels MCP s'exécutent sur le game thread. | Pour laisser le monde avancer, faire deux appels MCP séparés. |
| 4.6 | 🟠 | `ObjectTools.get_properties` ne lit pas les propriétés transientes (`Controller`, `PlayerInput`, `EnhancedActionMappings`) ; `set_properties` refuse les variables `BlueprintReadOnly`. | Juger par l'effet observable, pas par introspection. |
| 4.7 | ⚪ | Signatures incohérentes : `get_properties` prend `properties`, `set_properties` prend `values` (une **chaîne** JSON), `list_properties` prend `instance`. Idem dans `BlueprintTools` : `remove_variable` prend `name`, `set_variable_category` prend `variable_name`. | `describe_toolset` avant, en cas de doute. Ne pas supposer qu'un paramètre porte le même nom d'un outil à l'autre. |
| 4.8 | 🟠 | **On ne peut pas armer un état de jeu pour un test headless** : `set_properties` refuse toute variable Blueprint qui n'est pas `Instance Editable` (extension de 4.6), et aucun outil ne sait **appeler** une fonction Blueprint sur une instance PIE. Une mécanique déclenchée par input est donc invérifiable sans simuler la touche (4.3 / 4.4). | Se rabattre sur les **preuves indirectes** : (a) les valeurs cachées au `BeginPlay` correspondent-elles au DataAsset → le cache a tourné ; (b) une variable écrite **uniquement** par le Tick a-t-elle changé → le composant tick ; (c) les propriétés du CMC modifiées au `BeginPlay`. Puis **le dire** et déléguer le reste à Louis (R8). |
| 4.9 | 🔴 | **`SlateInspectorToolset.PressKey` n'atteint qu'une partie des actions, et le faux négatif ressemble à un bug de gameplay.** Avec `GameGetsMouseControl = true`, **`F3` (`IMC_Debug`, priorité 1) fonctionne** — `bDebugEnabled` bascule. Mais **`A` et `LeftControl` (`IMC_Gameplay`, priorité 0) ne déclenchent rien**, alors que le slide sur `LeftControl` est validé manette en main depuis le J4. | J5 : le dash ne partait pas sous `PressKey`. Diagnostic tentant : « le câblage d'`IA_Dash` est faux ». **Faux.** Le contrôle par `LeftControl` sur une mécanique déjà validée a montré que c'est l'outil qui ne passe pas. Une correction a été appliquée sur cette hypothèse avant qu'elle ne soit invalidée. | **Toujours exécuter un contrôle sur une mécanique déjà validée avant de conclure quoi que ce soit d'un `PressKey` négatif.** Ici : `LeftControl` → `Slide.lastSlideEndTime`. Si le contrôle échoue aussi, l'outil est hors de cause pour ton code : **ne modifie rien**, dis-le et délègue à Louis (R8). Corollaire de 4.1 : un négatif d'outil n'est pas une preuve de bug. |
| 4.10 | ⚪ | **Témoin de tick pour un composant sans variable qui bouge.** Un composant dont le Tick n'écrit que des valeurs stables (timers à 0, drapeaux `false`) est indistinguable d'un composant mort par introspection. | J5 : `BPC_Dash` au repos n'écrit que des 0. | **Fabriquer le témoin : écrire une valeur fausse depuis l'extérieur et vérifier qu'elle est corrigée.** Ici `FirstPersonCamera.FieldOfView = 42` → un frame plus tard `UpdateFOV` a remis **100**. Preuve directe que `BeginPlay`, `CacheTuning`, le prérequis de tick et `TickDash` tournent tous. Marche pour toute propriété **native** (`set_properties` refuse les variables Blueprint non `Instance Editable`, cf. 4.6). |
| 4.11 | ✅ | **Déclencher une mécanique en headless : la parade à 4.9.** `PressTouche` n'atteint que `IMC_Debug`. Il suffit donc d'y **mapper temporairement l'action sur une touche F**. Recette complète, qui a mis fin à 5 playtests d'hypothèses fausses au J5 : (1) `set_properties` sur `IMC_Debug.defaultKeyMappings` pour ajouter `{key: "F4", action: IA_X, triggers: []}` ; (2) `GameGetsMouseControl = true` (4.3) ; (3) **armer l'état et appuyer dans le MÊME `execute_tool_script`** — aucune frame ne s'écoule entre les deux, donc la vélocité injectée par `set_properties` sur le CMC est intacte à l'appui ; (4) lire les variables **persistantes** au tour suivant (`LastDashTime`, `DashEntrySpeed`…). | J5 : a prouvé en 3 appels que le dash mémorisait et restaurait correctement (`entry = exit = 2246`), ce que 4 raisonnements successifs avaient nié. | **Toujours restaurer l'échafaudage** : `IMC_Debug` à son état d'origine, `GameGetsMouseControl = false`, et toute variable passée `Instance Editable` pour le test remise à `false`. Le vérifier explicitement, pas le supposer. Pour observer *pendant* une fenêtre courte, `customTimeDilation` sur le pawn (0.01) ralentit son temps sans toucher à l'éditeur — mais replacer l'acteur, il sort de la map en quelques appels. |

| 4.12 | 🟠 | **On ne peut pas ralentir le temps de PIE par outil.** `WorldSettings.timeDilation` est refusé en écriture (*« could not be set »*, extension de 4.6, `BlueprintReadOnly`). `Pawn.customTimeDilation` est accepté **sans erreur** puis **relu à 1.0** — écriture silencieusement inefficace. La recette 4.11 mentionnait `customTimeDilation` : **elle ne fonctionne pas** avec cet outillage. | J6 : impossible de maintenir un wall ride (fenêtre de 2 s) le temps d'un aller-retour MCP (~20 s de jeu, piège 4.4) pour tester le wall jump. | **Pour observer une fenêtre courte, il faut l'allonger par le tuning, pas par le temps** : monter la clé de durée dans le DataAsset, faire le test, **restaurer et revérifier les valeurs une par une**. Et se méfier : au J6, neutraliser `WallRide_GravityScale` pour « figer » le joueur l'a fait **monter au-dessus du mur de 800 uu** à cause de `WallRide_UpwardBoost` → faux négatif `NoWall`. Un échafaudage qui modifie le tuning modifie **la physique testée** : toujours se demander ce que la valeur neutralisée faisait d'autre. |
| 4.14 | 🟠 | **`get_properties` sur une propriété absente fait échouer TOUT l'appel `execute_tool_script`, même enveloppé dans `try/except Exception`.** L'erreur n'est pas une exception Python : elle est remontée au niveau MCP après coup, et remplace le résultat du script. Le `run()` a bien fini, ses résultats sont perdus. | J7 : une sonde sur un composant `BillboardComponent` (`text`, `worldSize`) dans une boucle sur tous les composants d'un `TextRenderActor` a tué un script dont les 3 autres relevés étaient déjà faits. | Extension de 2.16 : **ne jamais sonder « au cas où » sur une liste de composants hétérogènes.** Cibler le composant exact, et faire les sondes incertaines dans un appel **isolé** dont on peut perdre le résultat sans conséquence. |
| 4.15 | 🟠 | **On ne peut pas orienter le regard en PIE par outil.** `AController::ControlRotation` est **ni lisible ni inscriptible** (*« the following properties could not be read/set »*) — ce n'est pas une `UPROPERTY` éditable. Et poser la rotation du **pawn** ne sert à rien : `APlayerController::UpdateRotation` appelle `Pawn->FaceRotation(ControlRotation)` **chaque frame**, ce qui l'écrase (`bUseControllerRotationYaw = true`). | J8quater : impossible de tester « tourner la tête pendant un wall ride », qui est **exactement** le bug à corriger. | **Le seul levier est le yaw de spawn** : `StartPIE` avec `startTransform.rotation` initialise la `ControlRotation`, et sans souris elle ne bouge plus. On choisit donc l'angle **avant** la manip, et on oriente la *mécanique* par rapport à lui (ici : injecter la vélocité vers `+X` ou vers `−X` pour obtenir 0° ou 180° d'écart au regard). Lire le yaw courant : `ActorTools.get_actor_transform` sur le pawn — avec `bUseControllerRotationYaw`, le yaw de l'acteur **est** le yaw du contrôleur. Extension de 4.6 et 4.8. |
| 4.16 | 🟠 | **Deux appels MCP successifs ne sont pas atomiques : des frames s'écoulent entre eux.** Armer un état en plusieurs appels (téléporter, puis injecter la vélocité) laisse le monde avancer entre les deux — le pawn tombe, glisse, atterrit. | J8quater, 1ᵉʳ essai d'accroche : joueur posé à **6 uu** du contact du mur. Il a heurté le mur avant la première évaluation de détection, sa vitesse latérale est tombée à 0, et `IsValidWall` (qui exige `Dot(VelDir, Normal) < 0`) a **refusé** l'accroche. `LastEndReason` est resté `"---"` : aucun ride, aucune erreur, et le diagnostic évident (« le correctif a cassé l'accroche ») aurait été **faux**. | (a) Tout l'armement dans **un seul** `execute_tool_script` (rappel de 4.11). (b) **Dimensionner la fenêtre d'approche, pas seulement la position** : il faut que la surface entre dans la portée de trace **plusieurs évaluations avant** le contact. Ici, partir à ~96 uu du contact avec 250 uu/s d'approche = 0.28 s dans la portée = ~9 évaluations à 33 Hz → accroche systématique. (c) Un `LastEndReason` resté à sa valeur initiale ne veut pas dire « la mécanique a échoué », il veut dire **« elle n'a jamais démarré »** — ce sont deux bugs différents. |
| 4.17 | 🔴 | **`ObjectTools.set_properties` sur un composant EN PIE relance le `BeginPlay` de tous les composants de l'acteur.** L'écriture passe par `PostEditChangeProperty` → ré-enregistrement du composant → l'acteur rejoue l'initialisation. **Même une écriture qui ne change rien** (poser `movementMode = MOVE_Walking` alors qu'il vaut déjà `MOVE_Walking`) la déclenche. Aucune erreur, aucun log, et l'état de jeu qu'on croyait armer est en partie **réinitialisé au même instant**. | J8quinquies, mesure directe : `Dash.lastDashTime` = **127.64**, un `set_properties` d'une valeur identique sur `CharMoveComp`, relecture = **−999** — la valeur écrite par `CacheTuning`, appelée **uniquement** depuis `EventBeginPlay`. Les caches de tuning, les timers et les compteurs des 4 `BPC_*` repartent donc de zéro. | **Ne jamais lire un compteur « avant / après » de part et d'autre d'un `set_properties`** : la comparaison est fausse. Corollaires : (a) la recette **4.11** (armer + appuyer dans le même appel) reste valable parce que `BeginPlay` est idempotent ici et se produit **avant** l'appui, mais tout relevé antérieur est perdu ; (b) pour prouver qu'une variable a été écrite par le **gameplay**, il faut une séquence **sans aucune écriture de propriété** entre l'action et la lecture — ici : appuyer sur la touche, puis relire, rien d'autre ; (c) un `lastDashTime` / `lastEndReason` retombé à sa valeur d'initialisation ne veut pas dire « la mécanique n'a pas tourné », il veut peut-être dire **« tu viens de la redémarrer »** (parent de 4.16). |
| 4.18 | ✅ | **Pour maintenir le joueur « en l'air » pendant plusieurs allers-retours MCP : `CMC.movementMode = "MOVE_Flying"`.** C'est la réponse à 4.12 (on ne peut pas ralentir le temps) quand la fenêtre à observer est un **état aérien**. En `MOVE_Flying` il n'y a ni gravité ni atterrissage (piège 6.15) et **`IsMovingOnGround()` reste faux indéfiniment** — la fenêtre de 0.5 s d'un vrai saut devient une fenêtre illimitée, sans toucher au tuning donc **sans modifier la physique testée**. `movementMode` est lisible **et** inscriptible, contrairement à `timeDilation` (4.12) et à `ControlRotation` (4.15). | J8quinquies : a prouvé en 3 appels que `bSurfaceTouchedSinceDash` reste `false` en l'air et que le dash est refusé alors que `dashCharges = 1` et `chargeTimer = 0` — impossible à mesurer sur un saut réel (4.4). | Deux précautions : (a) `DriveCMC` réécrit `MaxWalkSpeed` et `GravityScale` chaque frame mais **pas** `MovementMode` — vérifier que c'est encore vrai avant de s'appuyer dessus, en relisant `movementMode` au tour suivant ; (b) **chaque `set_properties` relance `BeginPlay` (4.17)** : n'écrire le mode qu'**une fois**, et faire toutes les lectures suivantes sans plus rien écrire. Pour revenir au sol, `MOVE_Walking` — mais le relevé qui suit ce retour est contaminé par 4.17, il faut donc **rejouer l'action** après. |
| 4.13 | ✅ | **Le meilleur témoin headless d'une mécanique, c'est un couple entrée/sortie mesuré.** Le J6 a validé tout le wall ride sans playtest en lisant `entrySpeed` / `rideSpeed` / `lastEndReason` après coup : `2517.94 → 2417.46` = `entry × SpeedRetention²` **au centième**, ce qui prouve d'un coup l'accroche, la lecture de `CMC.Velocity`, la décroissance, et 2 s de re-traces réussies. | — | Prévoir **dès l'écriture** une variable « raison de sortie » (`LastEndReason`, chaîne) en plus des compteurs. Elle distingue gratuitement `Duration` / `NoWall` / `Grounded` / `StateLost` / `WallJump` et transforme un test binaire en diagnostic. Coût : 1 variable, 1 paramètre. |

---

## 5. Assets, enums, input

| # | Gravité | Piège | Parade |
|---|---|---|---|
| 5.1 | 🔴 | **Un `IMC_*` créé par outil a ses mappings dans le tableau déprécié `Mappings`**, alors qu'UE 5.8 ne lit que `DefaultKeyMappings`. Contexte appliqué, tableau vide, **aucune erreur nulle part**. | Après création d'un IMC par outil, vérifier `DefaultKeyMappings`. Ne jamais laisser les deux tableaux remplis (double source de vérité). |
| 5.2 | 🔴 | **Aucun outil ne sait écrire les entrées d'un `UserDefinedEnum`**, ni créer une variable/pin typée enum. | Les `E_*` et `S_*` de `08_DATA_SCHEMAS` sont saisis **à la main par Louis**. Lui fournir une liste exacte, précise, en fin de journal. |
| 5.3 | 🟠 | Le flag **`Pure`** d'une fonction n'est pas exposé par l'outillage. | Cochage manuel par Louis. |
| 5.4 | 💀 | **Sonder une classe invalide via une factory fige l'éditeur** (modale bloquante) et tue la connexion MCP. | Ne jamais sonder une classe au hasard. |
| 5.5 | 🟠 | Des assets **homonymes** dans deux dossiers (`Content/Input/IA_Move` vs `OVERDRIVE/Player/Input/IA_Move`) font que l'éditeur lie les nœuds au mauvais asset, sans moyen de désambiguïser par outil. | Un seul emplacement par nom. Tout le contenu du projet vit dans `Content/OVERDRIVE/`. |
| 5.6 | 🟠 | Les modifications faites par outil restent **en mémoire de l'éditeur** : `git status` ne voit rien. | `AssetTools.save_assets([...])` avant tout `git add`. |
| 5.7 | ⚪ | Le format littéral d'un pin `Vector2D` est `(X=0.000000,Y=0.000000)`. `"0,0"` échoue à la compilation. | — |
| 5.8 | 🟠 | **Renommer un `IA_*` ne renomme pas la classe générée de son event node.** `AssetTools.move` met bien à jour l'`IMC` et la référence d'asset dans le nœud, mais le `type_id` du `K2Node_EnhancedInputAction` reste `…EnhancedInputActionIA_AncienNom`. Un `find_node_types` renverra donc l'ancien nom indéfiniment. | Après `IA_Sprint` → `IA_Walk` : le nœud pointait sur `IA_Walk.IA_Walk` et fonctionnait, mais s'annonçait encore `EnhancedInputActionIA_Sprint`. | **Supprimer puis recréer le nœud d'event** avec le nouveau `type_id` (`Input\|EnhancedActionEvents\|IA_NouveauNom`, cf. 2.14), et recâbler ses pins exec. Pas d'autre moyen. |
| 5.9 | 🟠 | **Aucun outil ne renomme un graphe de fonction Blueprint.** | — | Créer la nouvelle fonction, réécrire son corps, recréer les nœuds d'appel avec le nouveau `type_id` (`CallFunction\|NouveauNom`), recâbler, puis `remove_function_graph` sur l'ancienne. **Cartographier les sites d'appel avant** (`find_nodes` + `get_node_infos`, filtrer sur le `type_id`) : rien ne les liste automatiquement. |
| 5.10 | ⚪ | **`AssetTools.find_assets` renvoie des chemins de PACKAGE, pas des chemins d'OBJET.** Il rend `/Game/…/SM_Crate` ; tout outil qui prend un `refPath` typé (`StaticMeshTools.*`, `ObjectTools.*`) exige `/Game/…/SM_Crate.SM_Crate` et refuse le premier. | J6, audit des 41 meshes importés : *« … is not a valid object path for property 'mesh' »*, 41 fois. Le script s'exécute quand même (les erreurs sont attrapées) — mais un script non gardé aurait avorté (2.16). | Recomposer systématiquement : `path + "." + path.split("/")[-1]`. Le seul cas où le chemin de package suffit est `AssetTools` lui-même (`get_referencers`, `save_assets`, `delete`). |
| 5.11 | 🟠 | **`StaticMeshTools.import_file` n'expose PAS les options d'import FBX de `SPEC_ART_DIRECTION §7.4`** : ni `Generate Lightmap UVs`, ni `Auto Generate Collision`, ni `Build Nanite`. Les défauts du projet s'appliquent en silence. | 41 meshes importés au J6 : Nanite s'est bien retrouvé **off** et la collision **UCX du FBX** a été respectée, mais `lightMapCoordinateIndex = 1` sur les 41 → un canal UV de lightmap a été généré alors que le Static Lighting est désactivé. | **Relire après import**, jamais supposer : `is_nanite_enabled`, `bCustomizedCollision`, `lightMapCoordinateIndex` via `ObjectTools.get_properties`. `combine_meshes = False` est obligatoire (§7.4) et n'écrase pas la collision UCX. |
| 5.12 | ⚪ | **`ObjectTools.get_properties` renvoie une CHAÎNE JSON**, pas un dict — contrairement à la plupart des autres outils. | `'str' object has no attribute 'get'` dans un `execute_tool_script`. | `json.loads(r) if isinstance(r, str) else r`. Extension de 4.7 (signatures incohérentes). |
| 5.13 | 🔴 | **Blender → FBX → UE inverse l'axe Y.** Les deux moteurs sont Z-up mais de **main opposée** : une géométrie construite sur `Y ∈ [0, +D]` dans Blender arrive sur `Y ∈ [−D, 0]` dans UE. La longueur sur `+X` et la hauteur sur `+Z` sont préservées — **seul Y bascule**. Conséquence : le pivot d'un module rectangulaire tombe sur le coin `Y+`, alors que `SPEC_LEVELDESIGN §3` impose `X− Y− Z−`. | J6 : `SM_Module_TraversalStrip_400` construit `Y 0→20` arrive `min Y = −20`. Les dimensions sont justes, le nom est juste, le mesh est juste — **seul le pivot est du mauvais côté**, ce qui ne se voit qu'en lisant les bornes, jamais dans le Content Browser. | **Construire en Y négatif dans Blender** (`Y ∈ [−D, 0]`) pour obtenir un pivot `Y−` dans UE. Vérifier en lisant `get_bounds().min`, qui doit valoir `(0,0,0)` pour tout module à pivot coin. Ne jamais déduire l'orientation d'une doc : **faire un import-témoin sur le mesh le plus asymétrique du lot avant d'importer les autres.** |
| 5.15 | 💀 | **`bodyInstance.objectType` est INÉCRIVABLE, et écrire `collisionProfileName` ne charge PAS les données du profil.** Les deux écritures sont acceptées **sans la moindre erreur**. Le nom du profil se pose bien (relu : `OD_WallRideSurface`), mais l'`objectType` reste sur son défaut `ECC_WorldStatic` — testé sous **quatre** formes : dans le même struct que le profil, seul après, en recopiant le `bodyInstance` entier d'un mur qui fonctionne, et avec le profil forcé à `Custom` au préalable. `FBodyInstance::ObjectType` n'est pas `EditAnywhere` : il est piloté par `SetCollisionProfileName()`, que le setter générique n'appelle jamais. | J7 : un cube posé en `OD_WallRideSurface` pour la zone K. `trace_world` le touchait normalement, mais la requête physique `find_actors(collision_channels=["ObjectTypeQuery8"])` ne le voyait **pas** — donc `BPC_WallRide`, qui trace `ForObjects` sur ce seul canal, ne s'y serait **jamais** accroché. Un mur ridable en apparence, inerte en jeu, zéro warning. | **Ne jamais poser un preset de collision sur un composant d'acteur par outil. Le poser sur l'ASSET StaticMesh** (`bUseDefaultCollision`), où il survit à l'instanciation — c'est ce que fait le kit modulaire. Parade appliquée : remplacer le cube par deux `SM_Module_WallRide_1600`, dont le preset vit sur l'asset. **Et vérifier par requête physique, jamais par relecture de propriété** — avec un **contrôle sur un acteur déjà validé** (ici les murs de la zone E) pour prouver que c'est l'objet qui est en cause et pas le canal (4.9). |
| 5.16 | 🔴 | **`BlueprintTools.create` avec `asset_type = /Script/CoreUObject.Interface` NE crée PAS une Blueprint Interface.** L'appel réussit, renvoie un chemin d'asset valide, et produit une **Blueprint normale reparentée à `UInterface`** : `BlueprintType = BPTYPE_Normal`. Une telle Blueprint n'apparaît jamais dans le picker « Implement Interface » et ne génère aucun nœud de message. Zéro erreur, zéro warning. | J8 : `BPI_Damageable` créée en une ligne, `get_parent` répondait bien `/Script/CoreUObject.Interface`. **Le témoin qui a tranché est `list_graphs` : l'asset avait un `EventGraph`** — une vraie Blueprint Interface n'en a jamais. Confirmé ensuite par `AssetTools.get_asset_tags` → `BlueprintType: BPTYPE_Normal`. | **Contrôle après toute création de BP par outil : `list_graphs`.** Un `EventGraph` sur ce qui devait être une interface = raté. Et surtout : **il n'existe aucun outil pour créer une Blueprint Interface, ni pour faire *implémenter* une interface par une Blueprint** (pas d'`add_interface` dans les 52 outils de `BlueprintTools`). Les deux gestes sont **manuels, par Louis** — extension de 5.2 aux `BPI_*` et à la case « Implemented Interfaces ». En revanche, **une fois l'asset créé à la main, l'outillage reprend la main** : `add_function_graph` + `add_function_param` / `add_struct_function_param` / `add_object_function_param` écrivent parfaitement les signatures. |
| 5.17 | 🟠 | **Aucune factory de `UserDefinedStruct` non plus.** `BlueprintTools.create` ne fabrique que des Blueprints ; `add_struct_variable` / `add_struct_function_param` **consomment** un `ScriptStruct` existant mais n'en créent aucun. | J8, `S_DamageInfo`. Symétrique de 5.2 (enums). | Les `S_*` de `08_DATA_SCHEMAS §2` sont créés **et remplis à la main par Louis**, comme les `E_*`. Lui fournir la liste champ par champ avec le type exact. **Dès que l'asset existe, tout le reste est outillable** : le struct devient un `struct_type` valide pour les variables et les paramètres de fonction. |
| 5.18 | ✅ | **On peut PROUVER l'index d'un `TraceTypeQuery` / `ObjectTypeQuery` sans lancer le jeu.** `UCollisionProfile` masque (`Hidden`) les entrées d'enum non utilisées : un nœud **switch** ne crée donc d'exec pin que pour les canaux réellement déclarés. | J8 : `get_node_type_pins("Utilities\|FlowControl\|Switch\|SwitchonETraceTypeQuery")` → **exactement** `TraceTypeQuery1, 2, 3`. Comme `Visibility` et `Camera` sont les deux traces natives et que `Weapon` (`ECC_GameTraceChannel3`, `bTraceType=True`) est le **seul** canal de trace custom du projet → **`Weapon = TraceTypeQuery3`**. Contrôle croisé : le même appel sur `SwitchonEObjectTypeQuery` renvoie 8 pins, et `ObjectTypeQuery8 = WallRideSurface` est déjà validé en jeu depuis le J6 — l'algorithme d'indexation est donc confirmé, pas supposé. | Ne jamais deviner un `TraceTypeQueryN`. Faire le switch, compter les pins, recouper avec un canal déjà validé en jeu. `SceneTools.get_collision_channels()` ne liste que les **object types**, pas les trace types : il ne suffit pas. |
| 5.19 | 🟠 | **L'ordre des entrées d'un `UserDefinedEnum` est INVISIBLE à l'outillage** : tout s'appelle `NewEnumerator0..N`, y compris les pins d'un switch et le défaut d'un `Literal enum`. Impossible de savoir par outil que `NewEnumerator1` = `LaserHeadshot`. Extension de 5.2. | J8 : il fallait poser `Laser` / `LaserHeadshot` sur le pin `Type` d'un `Make S_DamageInfo`. `ObjectTools.list_properties` sur l'enum ne renvoie que `enumDescription`. | **Lire le `.uasset` en binaire.** Les libellés sont stockés en clair dans le `DisplayNameMap`, **dans l'ordre des index**, chacun suivi de `…DisplayNameMap(N - Value)…`. Un `grep` de chaînes ASCII sur `Content/…/E_Truc.uasset` donne la table index → nom en une commande. Vérifié sur `E_DamageType` : `0 = Laser`, `1 = LaserHeadshot`, …, `6 = Environment`. Poser ensuite `set_pin_value = "NewEnumeratorN"`. |
| 5.20 | 🟠 | **Une variable Blueprint tout juste ajoutée n'existe pas encore sur le CDO.** `ObjectTools.set_properties` / `get_properties` répondent *« the following properties could not be set/read »* pour une variable créée par `add_variable` à la seconde d'avant. | J8 : impossible de poser les défauts de `DebugLineDuration` & co. | **`compile_blueprint` d'abord, `set_properties` ensuite.** La réflexion ne voit la variable qu'après régénération de la classe. Frère de 2.19 (la base d'actions non plus n'est pas à jour). |
| 5.21 | 🟠 | **Les vrais noms de toolsets sont pleinement qualifiés, et `list_toolsets` les tronque.** Le client de secours `Saved/mcp.py` coupe la sortie de `list` à 6000 caractères — et c'est précisément la queue coupée qui contient `editor_toolset.toolsets.blueprint.BlueprintTools`, `…asset.AssetTools`, `…programmatic.ProgrammaticToolset`, etc. | J8 : `list` ne montrait que 25 toolsets Niagara/PCG/GAS ; conclusion tentante et **fausse** : « `BlueprintTools` n'est pas chargé ». | **Le message d'erreur d'un `describe_toolset` bidon liste TOUS les toolsets** (`describe_toolset("zzz")`). C'est la source fiable. Et appeler `describe`/`call` avec le nom **court** échoue : il faut `editor_toolset.toolsets.<module>.<Classe>`. |
| 5.25 | 💀 | **`bodyInstance.collisionEnabled` posé sur un composant de Blueprint est ÉCRASÉ à chaque recompilation tant que `bUseDefaultCollision` vaut `true`.** Le composant retombe alors sur la collision par défaut de l'**asset StaticMesh**. L'écriture réussit, la relecture immédiate confirme la bonne valeur — **et le premier `compile_blueprint` venu la défait**, sans erreur ni warning. C'est le pendant « écriture qui ne survit pas » du piège 5.15. | J8 : `WeaponMesh` du pistolet mis à `NoCollision` et **relu à `NoCollision`**. Après plusieurs recompilations du Blueprint dans la journée, retour à `QueryAndPhysics` / `BlockAllDynamic` → **l'arme bloquait la capsule du joueur et l'empêchait de se déplacer** (« ça collide avec ma capsule »). Rapporté par Louis manche en main, pas détecté par mes vérifications — parce que je vérifiais **avant** les recompilations suivantes. | **Toujours poser `bUseDefaultCollision = false` en même temps que `collisionEnabled`.** Et surtout : **la vérification n'est valable que si on RECOMPILE puis on relit.** Relire juste après l'écriture ne prouve rien sur une propriété adossée à un asset. Règle générale : pour toute propriété de composant qui a une « source par défaut » ailleurs (collision, matériaux, bounds), le test est *écrire → compiler → relire*, jamais *écrire → relire*. |
| 5.26 | 💀 | **La collision d'un composant de Child Actor ne se règle PAS par propriété. Il faut l'appeler dans le graphe — et seule une lecture EN PIE le prouve.** Trois couches mentent l'une après l'autre : (a) `bodyInstance.collisionEnabled` écrit sur le CDO **se relit correctement** ; (b) l'acteur en jeu est spawné depuis le **`ChildActorTemplate`** (l'instance s'appelle `…_BP_X_C_CAT_0`, `CAT` = *ChildActorTemplate*), pas depuis le CDO ; (c) purger le template (`childActorClass = None` → recompiler → réassigner) **régénère bien** certaines propriétés — `firstPersonPrimitiveType` est passé — **mais pas la collision**, parce que le `collisionProfileName` du composant (`BlockAllDynamic`) est **réappliqué à l'enregistrement** et écrase `collisionEnabled`. **Le profil gagne toujours contre la propriété.** | J8 : arme FP en child actor du joueur. Trois passes de correctifs par propriété, toutes « vérifiées », toutes relues bonnes dans l'éditeur — et **Louis restait bloqué, incapable d'avancer**, la capsule butant sur le pistolet. La lecture en PIE a donné en une fois `WeaponMesh -> QueryAndPhysics \| BlockAllDynamic` là où le CDO disait `NoCollision`. | **Poser l'appel dans le graphe** : `WeaponMesh.SetCollisionEnabled(NoCollision)` en **tête** de l'`EventBeginPlay` de l'acteur enfant, avant tout le reste (donc exécuté même si un cast plus bas échoue). Relu en PIE : `NoCollision \| profil Custom` — l'API force le profil en `Custom`, ce qu'aucune écriture de propriété n'obtient. **Et surtout, la règle de méthode :** pour un composant de child actor, `get_properties` sur le CDO **ne prouve rien**. Le seul contrôle valable est `StartPIE` → `find_actors` → `get_components` → `get_properties` sur l'**instance de jeu**. Généralisation de 5.25 : *écrire → compiler → relire* ne suffit pas non plus, il faut *écrire → **jouer** → relire*. |
| 5.27 | 💀 | **Une variable AJOUTÉE à une Blueprint enfant naît PÉRIMÉE dans le `ChildActorTemplate` du parent, avec la valeur qu'avait le CDO à l'instant de la recompilation — pas avec le défaut qu'on pose ensuite.** Le template est un objet sérialisé ; à la régénération de la classe il capture la valeur courante du CDO comme **delta explicite**. Poser le défaut sur le CDO **après** ne le rattrape jamais. Même mécanique pour `primaryActorTick`. | J8, correctif du faisceau : `DebugBeamDuration` relue **`0.12` sur le CDO** et **`0` sur l'acteur en jeu**. Pire, `bStartWithTickEnabled` : `true` sur le CDO, **`false` en jeu** — le `bCanEverTick` que le compilateur venait d'activer était inutile, **le Tick n'aurait jamais démarré**, et le faisceau n'aurait simplement pas existé. Zéro erreur, compilation verte, CDO parfait. | **Après toute variable ajoutée à une classe utilisée en Child Actor, écrire la valeur sur le `ChildActorTemplate`, pas seulement sur le CDO.** On l'atteint par `get_properties(composant, ["childActorTemplate"])` → `…:ChildActor_X_GEN_VARIABLE.ChildActor_X_GEN_VARIABLE_BP_Y_C_CAT`. Puis **le protocole de 5.26 s'applique intégralement** : écrire → recompiler **le parent ET l'enfant** → relire le template → **`StartPIE` → `find_actors` → relire sur l'instance de jeu**. Contrairement à la collision (5.26), un `float` et les flags de `primaryActorTick` **survivent** aux deux recompilations : vérifié. Détection bon marché : sur l'instance PIE, une variable **ancienne** juste (`debugLineThickness = 2`) à côté d'une variable **neuve** à zéro = signature du template périmé. |
| 5.28 | 🟠 | **Les noms de paramètres de `BlueprintTools` / `AssetTools` ne se devinent pas, et une seule faute avorte l'appel** (extension de 4.7 et 2.20). Aucun n'est celui qu'on écrirait spontanément. | J8bis, 5 allers-retours perdus : `add_variable` veut **`type_name`** (pas `type`) · `add_function_graph` veut **`graph_name`** (pas `name`) · `add_function_param` veut **`param_type`** + **`input_param`** (pas `type_name` / `is_output`) · `find_node_types` veut **`type_id_filter`** (pas `filter`) · `add_event` veut **`event_name`** (pas `name`) · `AssetTools.save_assets` veut **`asset_paths`, une liste de CHAÎNES** (pas `assets`, pas des `refPath`). **J8ter, 2 de plus : `write_graph_dsl` veut `code` (PAS `dsl`)** — et `AssetTools.find_assets` veut **`folder_path`** (pas `path`) **plus un `name` obligatoire**, fût-il vide. | Le message d'erreur **contient le schéma JSON complet** de la fonction : le lire au lieu de retenter au hasard. Et grouper les appels d'écriture dans un script **qui attrape les exceptions par appel** (jamais `execute_tool_script`, cf. 2.16) : sur ces 5 fautes, **aucune écriture partielle** n'a eu lieu, ce qui n'aurait pas été garanti autrement. |
| 5.24 | 🔴 | **Un journal d'asset n'est pas une mesure.** `Docs/Journal/2026-08-18_SM_Weapon_LaserPistol.md` annonçait « canon vers −Y Blender → **+X** dans UE ». Faux : les bounds réelles de `SM_Weapon_LaserPistol` dans UE sont `X 6.1 × Y 30.0 × Z 14.9`, canon à **`Y = +22.1`**. **La longueur est sur +Y.** C'est 5.13 (l'inversion Y de Blender→FBX→UE) appliquée à une arme au lieu d'un module : la doc décrivait l'**intention** d'export, pas le résultat. | J8 : `ChildActor_Laser` posé sans rotation → **l'arme visait à droite de l'écran**, et le composant `Muzzle` avait été placé à `(22.5, 0, 5)` — soit sur le **côté** du pistolet au lieu du bout du canon. Les deux erreurs viennent de la même phrase de doc, crue au lieu d'être vérifiée. | **Avant de placer ou d'orienter un mesh importé, lire ses bounds** (`StaticMeshTools.get_bounds`, paramètre `mesh` et non `static_mesh`). L'axe le plus long d'une arme est son canon : c'est une mesure à un appel. Parade appliquée : `yaw = −90°` sur `ChildActor_Laser` (aligne le +Y local sur l'avant +X) et `Muzzle` à `(0, 22.5, 5)` en espace mesh. Corollaire du principe n°1 de ce fichier, étendu à la doc : **un document qui ne renvoie pas d'erreur n'a pas forcément dit vrai.** |
| 5.22 | 🔴 | **Le `relativeScale3D` du composant RACINE d'un Blueprint EST l'échelle par défaut de l'acteur — et `SceneTools.add_to_scene_from_asset` l'écrase à `(1,1,1)` si le `xform` fourni n'a pas de champ `scale`.** La doc du paramètre dit « unset fields mean identity when creating objects » : *identity*, pas *valeur du Blueprint*. Aucune erreur, aucun warning. Le Blueprint garde la bonne échelle sur disque — **seules les instances placées sont fausses**, ce qui rend le contrôle « j'ai bien réglé le BP » rassurant et inutile. | J8 : `BP_TargetDummy` = cube moteur mis à `0.6 / 0.6 / 1.8` (60 × 60 × 180, taille d'un ennemi). Le composant du BP relu après coup disait bien `0.6, 0.6, 1.8`. Les 7 cibles placées mesuraient pourtant **100 × 100 × 100** et **flottaient 40 uu au-dessus du sol** — l'échelle Z de 1.8 étant tombée à 1, le calcul `Z = surface + 90` posait un cube de 100 à 40 uu de trop. | **Toujours passer `scale` explicitement dans le `xform` de spawn** quand le BP a un composant racine mis à l'échelle. Et surtout : **vérifier l'instance par `get_actor_bounds`, jamais le CDO** — c'est le seul contrôle qui voit la différence. Un `size` attendu et un `min.z` posé pile sur la surface valent tous les relevés de propriétés. Corollaire général : sur un composant racine, « échelle du composant » et « échelle de l'acteur » sont **la même donnée**, tout ce qui écrit l'une écrase l'autre. |
| 5.23 | ⚪ | **`SceneTools.add_to_scene_from_asset(name=...)` ne nomme pas l'acteur** : l'objet reçoit le nom de classe indexé (`BP_TargetDummy_C_0`…). `ActorTools.set_label` ne change que le **label** d'outliner, pas le nom d'objet — donc un chemin `PersistentLevel.<label>` reconstruit à la main est **invalide**. | J8 : `…PersistentLevel.TargetDummy_Flat_01 is not valid Actor`, alors que le label affiché en outliner est bien celui-là. | Ne jamais reconstruire un chemin d'acteur depuis son label. Repasser par `find_actors`, puis apparier avec `get_label` si on a besoin de savoir qui est qui. |
| 5.29 | ✅ | **Purger le corps d'un graphe de fonction AVANT de le réécrire en DSL supprime le risque d'empilement (2.2b/2.2c) — et c'est bon marché.** `delete_node` sur tous les nœuds sauf le `K2Node_FunctionEntry` laisse une fonction vide qui compile ; le `write_graph_dsl` qui suit ne peut alors produire qu'**une** chaîne, et le comptage après écriture devient une **mesure absolue** au lieu d'un delta à interpréter. | J8ter, `UpdateBeam` : 18 → purge → **1** → écriture → **34**, soit exactement l'attendu nœud par nœud. Aucune ambiguïté « a-t-il remplacé ou empilé ? ». | **Protocole** : (1) `find_nodes` ; (2) **imprimer la liste des nœuds à supprimer et vérifier qu'ils appartiennent tous au graphe visé** (le `refPath` contient le nom du graphe) — jamais de purge à l'aveugle (3.2) ; (3) supprimer ; (4) recompter (doit valoir 1) ; (5) écrire ; (6) recompter et auditer l'accessibilité exec. ⚠️ **Ne vaut que pour un graphe de FONCTION** dont on possède le corps entier. Sur un `EventGraph` contenant des events d'input, c'est la destruction 3.2 rejouée. |
| 5.30 | 🟠 | **`EditorToolset.EditorAppToolset` n'est PAS dans la liste `editor_toolset.toolsets.*`.** Les deux familles de noms coexistent : `editor_toolset.toolsets.<module>.<Classe>` (assets, blueprints, scène) **et** `<Famille>Toolset.<Classe>` (`EditorToolset.EditorAppToolset` pour `StartPIE`/`StopPIE`, `SlateInspectorToolset.SlateInspectorToolset` pour `PressKey`). Chercher `StartPIE` avec le préfixe `editor_toolset.` échoue par *« Toolset not found »*. | J8ter. | Extension de 5.21 : le **message d'erreur de `describe_toolset("zzz")`** est la seule liste complète, et il faut le lire **en entier** — le client `Saved/mcp.py` tronque `list`, et un `re.findall` sur `editor_toolset\.` seul **rate la moitié des toolsets**. |
| 5.14 | 🟠 | **`execute_tool_script` peut lever une exception qui ne concerne pas le travail demandé, alors que tout le travail a été fait.** L'erreur remontée citait un `TextRenderActor` du sandbox et la propriété `instance` — deux choses absentes du script. | J6 : le script d'import des 7 modules a renvoyé une erreur ; les 7 assets étaient pourtant tous créés. Conclure « l'import a échoué » et relancer aurait produit des doublons `_1`. | Corollaire de 2.23, étendu aux assets : **après toute erreur d'`execute_tool_script`, relire l'état réel avec un autre outil avant de retenter.** Ici `AssetTools.find_assets` sur le dossier cible a suffi. |
| 5.31 | 🔴 | **`MaterialInstanceTools.set_vector_parameter` n'applique AUCUNE conversion sRGB → linéaire.** Il écrit les composantes telles quelles. C'est le contraire du color picker de l'éditeur, qui convertit tout seul quand on colle un HEX dans l'onglet *Hex sRGB* — or **toute** la doc de couleur du projet (`PALETTE.md`, `SPEC_ART_DIRECTION`) est en HEX **sRGB**. Écrire `36/255, 40/255, 46/255` pour `#24282E` produit une couleur **~3× trop claire et délavée**, sans erreur, sans warning, et la relecture confirme fidèlement la mauvaise valeur. | J8ter, matériaux du pistolet. `#24282E` sRGB vaut `0.017642 / 0.021219 / 0.027321` en **linéaire** — et `0.141 / 0.157 / 0.180` si on divise bêtement par 255. Les deux « marchent ». | **Convertir avant d'écrire**, sans exception : `c ≤ 0.04045 ? c/12.92 : ((c+0.055)/1.055)^2.4` sur chaque composante normalisée. Et **écrire la valeur linéaire dans la doc à côté du HEX** — sinon le prochain agent recopiera le HEX. Vaut aussi pour `set_properties` sur un `defaultValue` de `MaterialExpressionVectorParameter`, et pour toute écriture de `LinearColor` par outil (widgets, `DA_UITokens`, `MPC_Global`). Corollaire : **il n'existe aucun outil MCP qui prenne un HEX.** Le seul endroit du pipeline qui convertit est l'UI de l'éditeur, que l'agent n'utilise jamais. |
| 5.32 | ✅ | **`MaterialTools` + `MaterialInstanceTools` sont fiables — recette complète d'un master paramétré, aucun piège rencontré.** Contrairement au DSL de graphe Blueprint, ces deux toolsets ne mentent pas : pas d'empilement, pas d'insertion silencieuse, `create_material` / `create` sont non idempotents mais explicites. | J8ter : `M_Weapon_Base` + 4 `MI_` + 4 slots posés du premier coup. | **Recette** : (1) `MaterialTools.create_material(folder_path, asset_name)` → renvoie le `refPath` d'objet directement utilisable ; (2) `add_expression(material_or_function, expression_class={refPath:"/Script/Engine.MaterialExpressionVectorParameter"}, x, y)` → renvoie le `refPath` de l'expression (`…M_X.M_X:MaterialExpressionVectorParameter_0`) ; (3) nom, groupe et défaut par **`ObjectTools.set_properties`** sur l'expression — champs `parameterName`, `group`, `sortPriority`, `defaultValue` ; (4) `connect_expressions` / `connect_to_output` ; (5) `layout_expressions` puis `recompile`. ⚠️ **Les noms de sortie ne se devinent pas** : un `VectorParameter` sort `["RGB","R","G","B","A","RGBA"]` (utiliser **`RGB`**), un `ScalarParameter` et un `Multiply` sortent `[""]` — **la chaîne vide est le nom valide**. Les sonder par `get_expression_output_names` / `get_expression_input_names` avant de câbler. **Contrôles obligatoires après coup, avec d'autres outils** : `MaterialTools.get_property_input` par pin (dit quelle expression alimente `MP_*`), `MaterialInstanceTools.list_parameters` sur le **master** (liste les paramètres exposés — un paramètre non branché à une sortie y apparaît quand même, donc ce n'est pas une preuve de câblage à lui seul), et `ObjectTools.get_properties` sur les `MI_` (`parent`, `scalarParameterValues`, `vectorParameterValues`) plutôt que les getters du toolset qui vient d'écrire. |
| 5.33 | 🟠 | **Les noms de slots de matériau d'un mesh se LISENT, ils ne se déduisent pas d'un journal d'asset** (application directe de 5.24). `StaticMeshTools.set_material` prend un `slot_name` : un nom faux fait échouer l'appel, mais un nom **juste dans le mauvais ordre** passe très bien et pose le mauvais matériau. | J8ter : les 4 slots de `SM_Weapon_LaserPistol` annoncés par le journal du 2026-08-18 se sont révélés exacts — mais c'est `get_material_slots` qui l'a prouvé, pas le journal. | `StaticMeshTools.get_material_slots(mesh)` **avant** toute assignation, et **relecture par `ObjectTools.get_properties(mesh, ["staticMaterials"])`** après : c'est le seul appel qui montre l'**ordre réel** (`materialSlotName` ↔ `materialInterface`), là où `get_material` répond slot par slot et ne dit rien de l'index. |

---

## 6. Pièges UE5 de gameplay (non liés à l'outillage)

Ceux-ci vivent aussi dans `Docs/Specs/SPEC_MOVEMENT.md §15`. Les deux plus coûteux à ce jour :

| # | Gravité | Piège | Parade |
|---|---|---|---|
| 6.1 | 🔴 | **Tout ce qui écrit `CMC.Velocity` doit s'exécuter AVANT l'écriture de `MaxWalkSpeed`.** `DriveCMC` placé avant l'air strafe calculait le plafond sur la vitesse d'avant le gain ; le CMC reclampait dessus à la frame suivante et effaçait le gain. Vitesse bloquée au sprint cap. | Ordre du Tick : toute écriture de vélocité → hard clamp → **`DriveCMC` en dernier**. Vaudra pour le dash (J5), le wall ride (J6), le bunny hop (J7). |
| 6.2 | 🔴 | **Un saut déclenché depuis `Event OnLanded` est détruit.** Juste après `Landed()`, le CMC appelle `SetPostLandedPhysics` → `SetMovementMode(Walking)` → **`Velocity.Z = 0`**. | Ne rien faire de physique dans `OnLanded` : poser un drapeau, consommer au Tick suivant (`ConsumeBufferedJump`). |
| 6.3 | 🟠 | Les constantes de Quake **ne sont pas transposables** telles quelles : Quake court à 320 u/s, OVERDRIVE à 1500 (facteur ≈ 4.7). | Rééchelonner `AirStrafe_WishSpeedCap` et `_SpeedGainPerSec` avec `Speed_SprintCap`. Cf. `07_TUNING §7`. |
| 6.4 | 🟠 | Deux modèles d'air strafe existent et **ne se jouent pas pareil** : Quake 1/CPMA (`wishspeed` bridé ~30 → strafe à la touche latérale **seule**) et Quake 3 (`wishspeed` = vitesse de course → **Z+Q / Z+D + souris**). | **OVERDRIVE utilise le modèle Quake 3** (`WishSpeedCap = Speed_SprintCap`), validé par Louis au J3. |
| 6.5 | 🔴 | **`Character::Crouch()` ne fait rien si `CharacterMovement.NavAgentProps.bCanCrouch` est faux** — et c'est le défaut. Aucun warning, aucune erreur : la capsule ne bouge simplement pas. | Cocher « Can Crouch » sur le CMC du Blueprint (ou `set_properties` sur `NavAgentProps`). **Vérifier ensuite `CrouchedHalfHeight` en PIE**, pas dans l'éditeur. |
| 6.6 | 🔴 | **`MaxWalkSpeedCrouched` (défaut 300) remplace `MaxWalkSpeed` dès que le personnage est accroupi** — un slide à 1900 uu/s se fait écraser à 300 sans que `DriveCMC` (qui n'écrit que `MaxWalkSpeed`) ne s'en aperçoive. | `BPC_Slide.CacheTuning` pose `MaxWalkSpeedCrouched = Speed_HardCap` au `BeginPlay`. Toute future mécanique accroupie doit vérifier ce doublon de plafond. |
| 6.7 | 🟠 | **Un composant qui tick avant `BPC_MovementState` voit l'état de la frame précédente.** Au frame d'atterrissage, `CurrentState` est encore `Falling` : un `RequestState(Sliding)` y est refusé (§1.3). Une action bufferisée consommée « une seule fois » à l'atterrissage ne part donc **jamais**. | Ne pas consommer le buffer au moment de la tentative : **réessayer chaque frame** tant que la fenêtre est ouverte, et vider le buffer seulement en cas de succès. Vaut pour le dash (J5) et le wall ride (J6). |
| 6.8 | 🔴 | **Une trace de sol calibrée sur la capsule *accroupie* rate le sol dès que la pente monte.** Sous le **centre** d'une capsule posée sur un plan incliné, le sol est à `(HalfHeight − Radius) + Radius / cos θ` — pas à `HalfHeight`. Avec `R = 34`, `HH = 88` : **88 uu à plat, 102 uu à 45°**. Une trace de `HalfHeight_Slide + MaxStepHeight = 94` **ne touche rien** à 45°, et n'a que **0.7 uu** de marge à 30°. | La normale retombe sur son défaut `(0,0,1)` → **plus aucune pente n'existe** pour le gameplay : pas d'accélération en descente, pas de freinage en montée, joueur figé accroupi sur une rampe. **Zéro erreur, zéro warning** — la fonction « marche », elle renvoie juste toujours le même résultat. | Calibrer la portée sur la capsule **debout** : `CapsuleHalfHeight + MaxStepHeight` (138 uu ici), ce qui couvre jusqu'à `WalkableFloorAngle` = 50°. **Vérifier le calcul avant d'écrire la trace**, pas après le playtest. Et afficher la valeur mesurée dans l'overlay de debug : une normale de sol qui vaut toujours `(0,0,1)` est invisible autrement. |
| 6.9 | 💀 | **Un plafond de vitesse piloté à 0 devient un softlock quand l'état qui le lève est inatteignable.** `MaxWalkSpeedCrouched = 0` implémentait « on ne peut pas accélérer accroupi » — mais coincé sous un plafond bas, on ne peut **ni se lever ni bouger**, définitivement. | Joueur bloqué au milieu du tunnel de la zone B, sans aucun message. | Tout plafond mis à 0 doit avoir une **échappatoire** : ici `bForcedSlide` remonte le plancher à `Speed_Walk` pour ramper dehors. Se poser la question « quel état lève cette contrainte, et est-il toujours atteignable ? » **avant** de brider à 0. |
| 6.10 | 🔴 | **`bCanWalkOffLedgesWhenCrouching` vaut `false` par défaut dans UE**, indépendamment de `bCanWalkOffLedges` (qui est à `true`). Accroupi, le CMC **refuse de franchir le moindre rebord**. | Debout on tombe normalement d'une plateforme ; **en slide on heurte un mur invisible** à chaque bord et en bas de chaque rampe. Aucune erreur, aucun log — c'est un comportement voulu du moteur, pour les IA et les jeux à couverture. | Cocher **« Can Walk Off Ledges When Crouching »** sur le CMC (ou `set_properties` sur le CDO). **À vérifier dès qu'une mécanique accroupie existe** — le symptôme ressemble à un bug de collision et fait chercher au mauvais endroit. |
| 6.11 | 🟠 | **Un trigger explicite change le pin d'event qui part.** Une entrée d'`IMC` **sans trigger** (`IA_Slide`, `IA_Jump`) émet `Started` à l'appui et `Completed` au relâchement. Une entrée avec un trigger **`Pressed`** explicite (`IA_Dash`, `IA_Fire`, `IA_Melee`) émet `Triggered`. Recopier le câblage `Started` du J4 sur une action à trigger `Pressed` n'est donc pas mécanique. | Aucune erreur, aucun warning : le graphe compile, l'action ne part simplement jamais si on a choisi le mauvais pin. | Lire `IMC_Gameplay.defaultKeyMappings` **avant** de câbler : le champ `triggers` de l'entrée dit quel pin utiliser. En cas de doute, **brancher `Triggered` ET `Started` sur la même cible** — les deux partent au même frame pour un trigger `Pressed`, et une fonction idempotente (gardée par ses propres conditions) absorbe le doublon sans effet de bord. C'est ce que fait `IA_Dash` au J5. |
| 6.12 | 🔴 | **Un composant qui tick en DERNIER lit des valeurs périmées d'un frame.** Le pendant exact de 6.7. `BPC_Dash` tick après `BPC_MovementState` pour avoir le dernier mot sur la vélocité (D32) — mais du coup **toute variable calculée par `BPC_MovementState` au même frame décrit l'état d'AVANT**. `HorizontalSpeed` en particulier. | J5 : `StartDash` mémorisait la vitesse d'entrée via `GetHorizontalSpeed()`. En enchaînant slide → dash → slide, la valeur lue était celle mesurée **pendant le dash précédent** : le joueur restait **bloqué à 5625 uu/s en permanence**, et le slide conservait fidèlement cette fausse vitesse pendant sa seconde de `Slide_HoldTime`. Aucune erreur, aucun warning — juste un jeu cassé. | **Ne jamais lire une valeur dérivée calculée par un composant qui tick avant toi : lire la source.** Ici `CMC.Velocity` au lieu de `MovementState.HorizontalSpeed`. Règle générale : un composant qui tick en dernier ne consomme que des **états moteur** (vélocité, `MovementMode`, `IsMovingOnGround`), jamais des **caches** d'un autre composant. Vaudra pour `BPC_WallRide` (J6), même ordre de tick. |
| 6.13 | 💀 | **Une mécanique qui écrit `CMC.Velocity` la publie pour TOUT LE MONDE.** Pendant les 0.16 s d'un dash, `CMC.Velocity` **vaut réellement 5625 uu/s** — ce n'est pas une valeur « privée » au dash. Tout composant qui lit la vélocité pendant ce temps l'hérite, et tout composant qui la **réécrit** la fait survivre au dash. | J5, signalé deux fois par Louis : « je peux spam dash en étant crouch et je vais à 5625 constamment ». `BPC_Slide.CrouchStep` (accroupi, sans slider) lit `CMC.Velocity`, applique une friction de seulement `Slide_Friction × 4 = 0.6/s` — soit ~1 %/frame — et **la réécrit**. Il tick **avant** le dash : il lisait donc 5625 chaque frame et l'entretenait. `SlideStep` faisait pire : sa fenêtre `Slide_HoldTime` **se réarme dès qu'elle détecte une accélération**, donc elle capturait les 5625 pour une seconde pleine. Zéro erreur, zéro warning. | **Une mécanique qui possède la vélocité doit la posséder *exclusivement*.** Le garde-fou est l'état, pas la politesse : `BPC_Slide.UpdateSlidePhysics` est devenue un `switch` sur `CurrentState` dont le pin **`Dashing` n'est connecté à rien** — ni `SlideStep` ni `CrouchStep` ne tournent pendant le dash. Se poser la question **avant** d'écrire une mécanique qui pilote la vélocité : *qui d'autre lit `Velocity` ce frame-là, et est-ce qu'il la réécrit ?* Vaudra pour `BPC_WallRide` (J6) et le bunny hop (J7). |
| 6.14 | 💀 | **Écrire une variable de travail que l'étape suivante recalcule = code mort silencieux.** `ApplyMomentumDecay` écrivait `HorizontalSpeed` (la variable). Deux étapes plus loin dans le même Tick, `ClampToHardCap` fait `SetHorizontalSpeed(VectorLengthXY(CMC.Velocity))` — il **recalcule** depuis la vélocité réelle et **écrase** la décroissance avant que quiconque ne la lise. **La décroissance de momentum n'a donc jamais eu le moindre effet, depuis le J2.** | Invisible pendant 3 jours : rien ne poussait durablement au-dessus du sprint cap. Le dash l'a révélé — `DriveCMC` posant `MaxWalkSpeed = max(HorizontalSpeed, cap)`, le CMC ne freine jamais, et toute vitesse excédentaire était **définitive**. « Je peux spam dash et revenir à max speed. » | **Une étape de Tick qui « corrige » une grandeur doit écrire la source de vérité, pas un cache.** Ici la source est `CMC.Velocity` : la décroissance met la vélocité à l'échelle, et ne renseigne `HorizontalSpeed` que pour l'affichage. **Test de relecture** : pour chaque `Set` d'une variable dans une boucle de Tick, chercher qui la réécrit **plus loin dans la même frame**. Si quelqu'un la recalcule depuis une source, ton écriture est morte. |
| 6.15 | 🔴 | **En `MOVE_Flying`, le CMC n'atterrit jamais.** Aucun `Landed`, aucun passage automatique en `Walking`, `IsMovingOnGround()` reste faux : le personnage **rase le sol en volant**. Toute mécanique qui bascule en `MOVE_Flying` doit détecter le sol **elle-même**. | J6, découvert **en vérification headless et non en playtest** : un wall ride qui descendait jusqu'au sol continuait à glisser dessus jusqu'à épuisement de `WallRide_MaxDuration` (2 s), joueur incontrôlable. `lastEndReason` disait `Duration` alors que le joueur était au sol depuis 0.6 s. | Ajouter une **trace vers le bas** dans la boucle de la mécanique, portée `CapsuleHalfHeight + MaxStepHeight` (même calibrage que 6.8), object types `[WorldStatic, WorldDynamic, …]` — et en faire une **condition de sortie nommée** (`Grounded`), pas un cas silencieux. Vaudra pour toute mécanique future en `MOVE_Flying`. |
| 6.17 | 💀 | **Le `BeginPlay` d'un Child Actor s'exécute AVANT `Possess()` : `GetController()` y renvoie NUL.** Le pawn du joueur est créé par `GameMode::RestartPlayer` → `SpawnDefaultPawnFor` (le monde ayant déjà démarré, `BeginPlay` part **à l'intérieur** du `SpawnActor`, composants et child actors compris) → **et seulement ensuite** `Possess()`. Toute mise en cache d'un `PlayerController` au `BeginPlay` d'un pawn ou d'un de ses child actors est donc nulle. **Et un getter appelé sur une référence nulle ne plante pas** : il renvoie la valeur par défaut du type, silencieusement. | J8 : `BP_LaserWeapon.EventBeginPlay` cachait `OwnerController = Cast<PlayerController>(GetOwner().GetController())`. Câblage **parfait**, vérifié nœud par nœud. En jeu, `OwnerController` nul → `GetPlayerCameraManager` nul → `GetCameraLocation` = **`(0,0,0)`** et `GetControlRotation` = `(0,0,0)` → forward `(1,0,0)`. **Tous les tirs partaient de l'origine du monde vers +X**, quel que soit le regard. Zéro erreur, zéro warning, et le graphe est innocent. | **Ne jamais cacher un `Controller` au `BeginPlay`.** Le résoudre **paresseusement, au premier usage** — au moment d'un input, la possession est forcément faite. Ici : fonction `EnsureOwnerRefs()` appelée en tête de `TryFire`, qui réassigne `OwnerController = GetPlayerController(0)` sans condition (idempotent, ~5 appels/s, pas de branche donc pas de 2.11). `OwnerCharacter` via `GetOwner()`, lui, est **valide** dès le `BeginPlay` : `ChildActorComponent` renseigne l'`Owner` au spawn. ⚠️ `SPEC_COMBAT §2` dit « refs cachées au `BeginPlay` » — **la spec a tort**, corrigée. Symptôme générique à retenir : **une grandeur spatiale qui vaut exactement `(0,0,0)` en jeu est presque toujours un getter sur un objet nul, pas un calcul faux.** |
| 6.18 | 🔴 | **`Draw Debug Line` avec `Duration = 0` ne dessine PAS une frame : il dessine 1 SECONDE.** `DrawDebugHelpers.cpp` : `GetDebugLineLifeTime()` renvoie `(LifeTime > 0.f) ? LifeTime : LineBatcher->DefaultLifeTime`, et `ULineBatchComponent::DefaultLifeTime = 1.0f`. Un `0` — qui est **le défaut du pin** — est donc silencieusement promu à 1 s. | J8 : la consigne du correctif était « durée de dessin nulle ou d'une frame ». Laisser le pin `Duration` à son défaut `0.000000` (donc sans écrire aucune valeur en dur, ce qui *paraissait* le geste le plus propre au regard de R3) aurait **empilé 60 lignes figées par seconde**, chacune vivant 1 s : soit exactement le bug qu'on corrigeait, en 60 fois pire. Zéro erreur, zéro warning. | ⚠️ **Correction J8bis : `DeltaSeconds` non plus ne marche pas.** Avec `Duration = DeltaSeconds` (~0.017 s), **plus aucun rayon n'est visible** — un segment dont la durée de vie est de l'ordre de la frame peut être expiré par le line batcher avant d'être rendu. La seule valeur qui fonctionne est une **clé de tuning franchement supérieure à une frame** : `LaserDebug_DrawLifetime` = 0.05 s. Le pin `Duration` n'a donc **que des mauvaises réponses évidentes** — ni 0, ni `DeltaSeconds`. Règle générale : **un `0` sur un pin de durée moteur ne veut pas dire « instantané » — il veut souvent dire « défaut du système ».** Vérifier dans la source (`Engine/Source/Runtime/Engine/Private/`) avant de s'appuyer sur un zéro ; ici deux `grep` ont suffi. Corollaire du principe n°1 : **un défaut de pin n'est pas une valeur neutre.** |
| 6.19 | ✅ | **L'alpha d'un `Draw Debug Line` est bien respecté — vérifié, pas supposé.** `FLineBatcherSceneProxy::GetDynamicMeshElements` appelle `PDI->DrawTranslucentLine(...)` et déclare `bNormalTranslucency = true` ; `FLinearColor::ToFColor(true)` préserve le canal A. Un fondu par alpha est donc réalisable sans Niagara. | J8 : le fondu du faisceau de debug repose entièrement dessus. L'alternative envisagée — faire décroître le RGB — aurait été **fausse sur ce jeu** : sur un monde `OD_White_Structure` en plein jour, un faisceau qui tend vers le noir devient **plus** visible, pas moins. | Avant de choisir un mode de fondu, se demander vers quoi la valeur tend **sur le fond réel du jeu** (`SPEC_COMBAT §3.2` : fond clair). Et pour toute question « est-ce que le moteur honore ce canal ? », la source est à deux `grep` — c'est moins cher qu'un playtest raté. |
| 6.20 | 🔴 | **Une origine relue chaque frame + une durée de vie de segment > 1 frame = ÉVENTAIL de segments, lu par le joueur comme « deux rayons ».** Les deux réglages sont individuellement corrects et se contredisent : `LaserDebug_DrawLifetime` = 0.05 s fait vivre ~3 segments simultanés, et à 1900 uu/s le muzzle bouge de ~32 uu entre deux frames. **Le correctif du piège précédent (6.18/§13-10, « redessiner depuis le muzzle courant ») fabrique donc le bug suivant.** | J8bis, Louis manche en main : *« je vois deux rayons qui divergent depuis le point d'impact »*. Zéro erreur, zéro warning, et les deux versions « fonctionnent » au sens du compilateur. Impossible à voir sans jouer : à l'arrêt le bug n'existe pas. | **Borner la fenêtre pendant laquelle l'origine est relue** (`LaserDebug_AttachTime` = 0.05 s), puis **figer l'origine en espace monde** — accroche puis décrochage. Règle générale, valable pour tout tracé persistant attaché à un porteur mobile (beam, trail, decal de debug) : **origine mobile ET segment persistant sont incompatibles, il faut borner l'une des deux.** Corollaire de méthode : quand on corrige un bug de rendu lié à la vitesse, **se demander tout de suite ce que le correctif produit à 2000 uu/s**, pas seulement à l'arrêt. |
| 6.21 | 🔴 | **`bUseControllerRotationYaw = true` fait que TOUT vecteur d'acteur suit le regard. Une trace de gameplay bâtie sur `ActorRightVector`/`ActorForwardVector` est donc une trace « qui suit la souris » — et ça ne se voit dans aucun graphe.** Le symptôme se présente sous une forme trompeuse : la mécanique s'arrête avec une raison **plausible** (`NoWall`), jamais avec une erreur. | J8quater : `BPC_WallRide.ConfirmWall` traçait le long de `ActorRightVector × WallSide`. Tourner la tête réorientait la trace de maintien → elle sortait du mur (≈ **67°** avec un standoff de 40 uu et 104 uu de portée) → `MissedTraces ≥ tolérance` en **0.06 s** → `EndWallRide("NoWall")`. Invisible tant qu'il n'y avait pas d'arme ; dès le J8, viser sur le côté décrochait du mur. | **Pour MAINTENIR un contact, tracer le long d'une donnée du MONDE (la normale mémorisée), jamais le long d'une base de l'acteur.** L'acteur n'est légitime que pour la **détection initiale** (« y a-t-il un mur à ma droite ? »). Règle générale : se demander pour chaque trace *« est-ce que ce vecteur change quand le joueur bouge la souris, et est-ce que je le veux ? »*. Vaudra pour toute future mécanique de contact (rebord, grappin, lock-on). |
| 6.22 | 🔴 | **Un `WishDir` en espace monde n'est PAS un « vecteur d'input » : c'est un vecteur d'input DANS LA BASE DU REGARD.** Il est correct pour le déplacement (l'air strafe) et **faux** dès qu'on veut juger d'une touche par rapport à une géométrie fixe. Les deux usages se ressemblent au point qu'on réutilise le mauvais par réflexe d'économie. | J8quater : la sortie `InputAway` du wall ride comparait `Dot(WishDir_monde, WallNormal)` à 0.7. En tenant `Z` — le réflexe pendant un ride — tourner la tête de **30°** donnait `sin(30°) = 0.5` puis `> 0.7` à 45° : **le joueur se décrochait en visant, sans avoir changé de touche.** Corriger la trace (6.21) ne l'aurait pas réglé : deux causes indépendantes produisaient le même symptôme, l'une masquant l'autre. | **Quand la question est « quelle TOUCHE le joueur presse-t-il par rapport à cette surface ? », reconstruire l'input dans le repère de la SURFACE** : `wish = Axe_le_long × MoveInput.Y + Cross(WorldUp, Axe_le_long) × MoveInput.X`. `D` seul donne alors `dot = ±1`, `Z+D` `±0.707`, `Z` seul `0` — indépendamment du regard. Et corollaire de méthode : **quand un symptôme a deux causes plausibles, les traiter toutes les deux ou prouver que la seconde n'existe pas** ; s'arrêter à la première laisse un bug qui ressemble à une régression du correctif. |
| 6.16 | 🟠 | **La matrice d'interactions et la table d'états peuvent se contredire — et c'est presque toujours la table qui a raison.** `SPEC_MOVEMENT §1.3` donnait `Dashing → WallRiding = oui`, `§11` disait « Wall ride pendant Dash : refusé ». | J6. Le J5 avait déjà vécu la même contradiction sur `Dashing → Sliding` (D33 puis D42) et **modifier la table avait été une erreur**. | **Ne pas toucher `CanEnterState`.** La cellule est un *canal de transition* ; l'interdiction d'une mécanique s'implémente là où la mécanique démarre — ici, la détection ne tourne que si `CurrentState ∈ {Jumping, Falling}`. Vérifier la table **avant** de la corriger : au J6 les 8 cellules `WallRiding` étaient déjà justes, l'écriture prévue était inutile. |
